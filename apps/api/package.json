{
  "name": "@bigant/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "esbuild src/server.ts --bundle --platform=node --format=esm --outfile=dist/server.js --external:fastify --external:@fastify/* --external:@prisma/client --external:argon2 --external:jose --external:dotenv --external:zod",
    "typecheck": "tsc --noEmit",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@bigant/database": "workspace:*",
    "@bigant/types": "workspace:*",
    "@bigant/i18n": "workspace:*",
    "fastify": "^5.7.4",
    "@fastify/cookie": "^11.0.2",
    "@fastify/rate-limit": "^10.3.0",
    "@fastify/helmet": "^13.0.2",
    "jose": "^6.1.3",
    "argon2": "^0.44.0",
    "dotenv": "^17.3.1",
    "@prisma/client": "6.19.2",
    "zod": "^4.3.6",
    "@bigant/core": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
